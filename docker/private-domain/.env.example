COMPOSE_PROJECT_NAME=paseo-domain
GATEWAY_HOSTNAME=paseo-domain
PUBLIC_HOSTNAME=paseo.example.com
# Existing private HTTPS service, without a scheme or path.
UPSTREAM_HOST=application.example-tailnet.ts.net
UPSTREAM_PORT=443
